const uName = 'Max';
let age = 25
const hobbies = true;

function summarizeUser(username, userAge, userHobby) {
    return (username+" : "+userAge+" : "+userHobby)
}

console.log(summarizeUser(uName, age, hobbies));